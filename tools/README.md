# tools（制品目录）

## plugin_generator 使用说明

- 工具目录：`tools/plugin_generator/`
- 可执行文件：`tools/plugin_generator/plugin_generator.exe`
- 这是已打包 Qt 运行时依赖的版本，可在未安装 Qt 开发环境的机器上直接双击运行。

## 注意

- 请保持 `tools/plugin_generator/` 目录完整，不要只拷贝单个 exe。
- 如需更新工具版本，请从 `osgi_core` 执行分发脚本后同步本目录。